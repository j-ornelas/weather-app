/* dev note: ideally in a real app, any private API keys would NOT be included
in the app bundle. We'd most likely make a request to a server that had this key in a .env file,
but that's outside the time scope of this demo.*/

export const WEATHER_KEY = '<YOUR API KEY HERE>';
